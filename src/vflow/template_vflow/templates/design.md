# 设计（SWE.2 架构 + SWE.3 详细设计）

## 架构影响（SWE.2）

<!-- 本任务对模块结构/接口/数据流的影响。轻任务一行"无架构影响"即可 -->

无架构影响

## 方案概述

<!-- 一段话说清思路 -->

## 改动清单

| 文件 | 改动类型 | 说明 |
| :--- | :--- | :--- |

## 关联规范（实现与审查按此定向加载）

<!-- 列出本任务实现/审查需读的 spec 文件及理由；实现前按此加载，审查对照，跨会话恢复从这里开始 -->

| spec 文件 | 适用理由 |
| :--- | :--- |

## 关键设计决策（ADR-lite）

<!-- 每条决策三段式：Context（背景/约束）→ Decision（选什么、备选为何不选）→ Consequences（代价/风险/后续影响）。轻任务可省略本节 -->

## 测试方案（必填）

<!-- 新增哪些测试用例、放哪个目录、覆盖哪些边界。
     如需缩小机器验证范围，在此声明 test_scope 并执行：task.py set test_scope "<命令>" -->

| 用例 | 验证什么 | 位置 |
| :--- | :--- | :--- |

## 任务清单（SWE.3，实现阶段逐项勾选）

<!-- 拆成可独立验证的小步。每项行尾必须标注覆盖的 R-ID，格式 "(R1)" 或 "(R1,R3)"。
     全部 R-ID 必须被覆盖，否则 advance 被拒。跨会话续做以此为断点 -->

- [ ] 1.1  (R1)

## 风险与审批

- 风险级别：low / high（理由：）
- 审批记录：<!-- 高风险任务记录用户确认的原话和时间 -->
